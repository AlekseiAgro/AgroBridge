import { NotFoundPanel } from '@/components/NotFoundPanel';

export default async function RequestsNotFound() {
  return (
    <main className="page__main">
      <NotFoundPanel />
    </main>
  );
}
