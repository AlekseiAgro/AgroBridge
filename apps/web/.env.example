# Local: API origin including /api
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Production: public API URL (baked into the Next.js client bundle at build time).
# Current production: https://api.agrobrid.ge/api — change + rebuild web when moving domains.
# Ready-to-paste values: ./scripts/domain-cutover.sh new-domain.example
# See docs/DOMAIN.md
