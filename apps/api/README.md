# @agrobridge/api

NestJS API for AgroBridge.

- Health: `GET /api/health`
- Prisma schema: `prisma/schema.prisma`
- Local env: copy `.env.example` → `.env`

```bash
pnpm --filter @agrobridge/api dev
```
